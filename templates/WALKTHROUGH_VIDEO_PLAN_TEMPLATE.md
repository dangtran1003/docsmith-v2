# Walkthrough Video Plan: [Product/Project Name]

Plan generated by the `record` command before video capture. Mirrors the screenshot capture plan but for `<!-- VIDEO ... -->` markers.

## Plan Metadata

- **Generated**: [date]
- **Source docs**: [path or glob]
- **UI version**: [e.g. console-v3.2]
- **Total markers found**: [N]
- **Markers blocked (incomplete fields)**: [N]
- **Estimated total recording time**: [minutes]

## Capture Plan

Ordered by navigation sequence so all videos in a feature area are recorded together. Reuses the same browser session as `walkthrough` when possible.

| # | Video ID | Type | Doc Path | Section | Duration | Start State | End State | Status |
|---|---|---|---|---|---|---|---|---|
| 1 | instance-quickstart-tour | tour | docs/drafts/getting-started.md | Quick Start | 75s | Compute > Instances, empty state | Instance list with 1 running instance | [Pending / Recorded / Failed] |
| 2 | volume-attach-procedure | procedure | docs/drafts/attach-volume.md | Steps | 45s | Volumes list, 1 detached volume | Volume row showing "Attached to demo-vm" | [Pending] |
| 3 | ... | | | | | | | |

## Per-Marker Detail

For each marker in the plan, expand the full spec:

### Video #1 — `instance-quickstart-tour`

- **Marker location**: `docs/drafts/getting-started.md`, line 23
- **Test case link**: TC-DOC-GS-002, TC-DOC-GS-003 (overlapping flow)
- **Output file**: `videos/instance-quickstart-tour.mp4`
- **Raw file**: `videos/raw/instance-quickstart-tour.mov`
- **Validation**:
  - [x] `id` present and unique
  - [x] `duration` ≤ 120s
  - [x] `start` describes page + state
  - [x] `actions` are unambiguous with data
  - [x] `end` describes page + state
- **Recording sequence**:
  1. Open browser, navigate to Compute > Instances
  2. Verify empty state (no instances)
  3. Begin recording
  4. Execute actions per marker (slow pacing on form fill, fast on wait)
  5. Stop recording when end state visible
  6. Save raw → transcode → save mp4

### Video #2 — `volume-attach-procedure`

(Same structure as above)

## Blocked Markers

Markers that cannot be recorded automatically. Listed with reason.

| Marker ID | Reason | Action |
|---|---|---|
| `historical-billing-tour` | Requires 6 months of usage data — cannot reproduce | Capture manually with real account, place file at `videos/historical-billing-tour.mp4` |

## Execution Notes

After running the plan, fill in:

- **Started**: [timestamp]
- **Completed**: [timestamp]
- **Markers recorded**: [N]
- **Markers skipped**: [N]
- **Markers failed**: [N]
- **Failures**:
  - `<id>`: [reason — e.g. UI label "Create" no longer exists]

Each failure should also produce a test case failure in the walkthrough execution record where applicable.
